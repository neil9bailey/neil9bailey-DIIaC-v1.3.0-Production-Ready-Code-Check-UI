# DIIaC v1.2.0 — Python Runtime Dependencies
# Full pinned manifest. Regenerate with:
#   pip-compile --generate-hashes pyproject.toml
# Audit with:
#   pip-audit -r requirements.txt

# ── Core runtime ──────────────────────────────────────────────────────────────
Flask==3.1.0
cryptography==44.0.1

# ── Flask transitive dependencies ────────────────────────────────────────────
Werkzeug==3.1.3
click==8.1.8
itsdangerous==2.2.0
Jinja2==3.1.5
MarkupSafe==3.0.2
blinker==1.9.0

# ── Cryptography transitive dependencies ──────────────────────────────────────
cffi==1.17.1
pycparser==2.22

# ── Development / test dependencies (not installed in production image) ───────
# Install with: pip install -r requirements-dev.txt
# or:           pip install -e ".[dev]"
